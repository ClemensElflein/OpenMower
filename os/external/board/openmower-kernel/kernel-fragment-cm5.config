# CM5/bcm2712-specific addendum to kernel-fragment-common.config.
#
# Distinct release string -- see kernel-fragment-cm4.config's comment for
# why (same reasoning, mirrored).
CONFIG_LOCALVERSION="-cm5"
