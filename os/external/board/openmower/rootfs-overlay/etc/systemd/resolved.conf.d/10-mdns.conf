# Global default -- resolved falls back to this on any link without its own
# explicit per-link setting (br0, wireless -- the only two interfaces that
# carry L3 config at all -- don't set their own anymore, this covers both).
[Resolve]
MulticastDNS=yes
