# HOSTNAME only feeds the link Dockge displays for this stack in its UI --
# it doesn't drive anything else. Rewritten automatically on every start
# (entrypoint.service), same trick as /data/stacks/webterminal/.env; edits
# here won't stick.
HOSTNAME="openmower.local"
